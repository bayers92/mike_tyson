<%= simple_form_for(@domain) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :url, label: "Enter desired domain name:" %>
    <%= f.association :user, as: :hidden %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
